create a migration for stored procedures for main modules or transactions in my app, also use triggers, indexing, events, to imporve the performance of my app, specifically for faster loading of data, faster searching, faster filtering, faster sorting, faster deletion, faster update, faster creation, and other queries that will help the app to run faster, for the database it self, use the mysql engine and database that we have in my local machine, 
- i have seqeuelize cli and i have models for all the modules and transactions in my app, so you can use the models to create the stored procedures, triggers, indexing, events, and other queries that will help the app to run faster, 

read the `GEMINI.md` for more information about the project and the database structure.

make sure that all the stored procedures, triggers, indexing, events, and other queries are well-documented and easy to understand, also make sure that they are well-tested and they are working as expected. and has data consistency with the other modules and transactions in the app.

make sure the the triggers only have minimal operations to prevent the app from being slower.

create a migration for that in the migration folder, and also use the sequelize cli to create the stored procedures, triggers, indexing, events, and other queries, and also use the sequelize cli to create the migration for that, make sure that the migration is well-documented and easy to understand, also make sure that the migration is well-tested and it is working as expected.
